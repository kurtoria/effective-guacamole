<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:labb"
             x:Class="labb.MainPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Margin="20,60,20,0" Text="HÖJ" HorizontalOptions="Center"/>
            <Button Margin="60,20,60,0" Text="Use button" Command="{Binding AddCommand}"/>
            <Button Margin="60,10,60,0" Text="Use button" Command="{Binding AddCommand}"/>
            <Button Margin="60,10,60,0" Text="Use button" Command="{Binding AddCommand}"/>
            <Button Margin="60,10,60,0" Text="Use button" Command="{Binding AddCommand}"/>
        
            <StackLayout Orientation="Horizontal"  HorizontalOptions="FillAndExpand">
                <Button Margin="20,10,20,0" HorizontalOptions="FillAndExpand" Text="bak" Command="{Binding AddCommand}"/>
                <Button Margin="20,10,20,0" HorizontalOptions="FillAndExpand" Text="fram" Command="{Binding AddCommand}"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>